# Plan J3 Après-midi - Éradication de la Dette Technique (Rapport Complet)

## 1. Nettoyage des Scripts CLI
- [x] Remplacer le `console.error` simple par un vrai throw ou une gestion propre dans `scripts/calculate-splits.ts`.
- [x] Nettoyer les regex ou commentaires contenant le mot-clé interdit dans `scripts/audit-dette.ts`.
- [x] Supprimer les imports inutilisés détectés dans les deux scripts.

## 2. Refactoring de l'UI & Formulaires
- [x] Découper les formulaires de Gear (`GearForm`, `EditGearForm`) et centraliser la logique commune pour supprimer la duplication de 11 lignes.
- [x] Découper toutes les fonctions d'UI > 50 lignes (Dashboard, Gear, Sessions, SplitsResult).

## 3. Optimisation de l'Architecture & Logs
- [x] Nettoyer les `console.log` de production (les remplacer par des logs d'erreur uniquement ou les retirer).
- [x] Aplatir le nesting profond (> 4 niveaux) dans les Server Actions (`sessions.ts`, `notifications.ts`) avec des clauses de garde (early returns).