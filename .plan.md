# Plan J4 Matin — Finalisation & Verrouillage Niveau Excellence

> Contrat d'exécution validé avant chaque session de développement.
> Chaque tâche est bloquée tant que `npm test && npx tsx scripts/audit-dette.ts` ne retourne pas `exit 0`.

---

## Statut fin J3 (acquis)

- [x] Stack Docker opérationnelle (`docker compose up --build` → 200 sur toutes les routes)
- [x] Migration SQLite via `migrate.mjs` au runtime (ADR-007)
- [x] `force-dynamic` sur les 3 routes de données (ADR-008)
- [x] Interface `/admin/logs` accessible et listée dans la nav
- [x] Lint clean : 0 erreur ESLint (fix `GearForm.tsx` — setState in effect)
- [x] Audit dette : score 100 % — zéro violation sur 27 fichiers
- [x] SKILL.md documenté (4 skills)
- [x] DECISIONS.md à jour (ADR-001 → ADR-008)

---

## Tâches J4 Matin

### 1. Tests — Couverture critique (priorité haute)
- [ ] Exécuter `npm test` et valider que les 39 tests passent
- [ ] Ajouter des tests pour `src/lib/actions/sessions.ts` : atomicité transaction (mock Drizzle)
- [ ] Ajouter des tests pour les helpers de validation : `validateRpe`, `validateDistance`, `validateDuration`
- [ ] Gate CI : `npm test` doit être `exit 0` avant tout commit final

### 2. TypeScript strict — Type-check complet
- [ ] Lancer `npx tsc --noEmit` et corriger les éventuelles erreurs résiduelles
- [ ] Vérifier l'absence de `@ts-ignore` et `@ts-expect-error` non justifiés

### 3. README — Relecture finale
- [ ] Vérifier que le README couvre : pitch, lancement dev, lancement Docker, accès `/admin/logs`
- [ ] Mettre à jour le score d'audit dans la section "Gouvernance & Alignement IA" si nécessaire

### 4. Git — Commit de clôture J3
- [ ] `git add` des fichiers modifiés aujourd'hui
- [ ] Commit avec message : `docs: finalize J3 — Docker ADRs, SKILL.md, export-logs, lint fix`
- [ ] Vérifier que le pre-commit hook (audit-dette) passe

### 5. Démo — Validation end-to-end
- [ ] `docker compose up --build` et vérifier :
  - Dashboard (`/`) : données affichées
  - `/sessions` : ajout et suppression fonctionnels
  - `/gear` : ajout fonctionnel, alertes visibles
  - `/admin/logs` : entrées de log visibles après actions
  - `/tools` : calculateur de splits (CLI + UI cohérents)
