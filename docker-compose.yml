services:
  cadence:
    build: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      DATABASE_URL: "file:/app/data/local.db"
      WEBHOOK_URL: ${WEBHOOK_URL}
    volumes:
      - cadence_data:/app/data

volumes:
  cadence_data:
